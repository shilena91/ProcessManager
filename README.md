## A MacOS ProcessManager application, written in UIKit

• Displays a list of running processes and applications (also processes not owned by the current user).
• Allows the user to kill processes that they owned.
• Automatically keeps the list updated as processes start and exit.
• Shows application error messages as alerts of some kind.
